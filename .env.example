# Required: Sentry self-hosted base URL
SENTRY_UPSTREAM=https://sentry.example.com

# Optional: Comma-separated list of allowed project IDs (empty = allow all)
ALLOWED_PROJECTS=1,2,3

# Optional: Server listen address (default: :8100)
LISTEN_ADDR=:8100

# Optional: Trust X-Forwarded-For header, enable behind a reverse proxy (default: false)
TRUST_PROXY=false

# Optional: Custom User-Agent for upstream requests (default: sentry-tunnel/1.0)
USER_AGENT=sentry-tunnel/1.0
